Contents:

- Document programmer's intent, concisely and at all levels: project
  description, source file headers, functions, etc. Produce comments or
  descriptions next to the relevant code whenever possible rather than
  in separate readmes.
- Cite references to any specification or source of inspiration that was
  consulted while writing code (title + URL).
- Generously reference other parts of the project that may be relevant. The
  simplest way looks like "See also: <blurb + file paths>".

Code formatting:

- Don't try to align text field boundaries vertically using whitespace padding
  (it's hard to maintain).
- Use American English.
